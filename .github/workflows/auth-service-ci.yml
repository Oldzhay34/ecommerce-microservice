name: Auth Service CI

on:
  push:
    branches: [ "main", "master" ]
    paths:
      # Sadece PromptEngineering klasörü veya bu workflow değiştiğinde tetiklenir
      - 'PromptEngineering/**'
      - '.github/workflows/auth-service-ci.yml'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'PromptEngineering/**'
      - '.github/workflows/auth-service-ci.yml'

jobs:
  test-and-build:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Kodu GitHub'dan Çek
        uses: actions/checkout@v4

      - name: Java JDK Kurulumu
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Spring Boot Testlerini Çalıştır
        # Doğru klasör ismi verildi: PromptEngineering
        working-directory: PromptEngineering
        run: mvn test -Dspring.profiles.active=test